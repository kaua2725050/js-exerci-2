//10. Faça um programa que receba um número do usuário e continue
//a pedir o número se o numero for diferente de 0.
console.log("-------------------------------------")

let nota = prompt("Digte: 0")

while (nota !=0) {
    console.log("isso não é 0")
    nota = prompt("eu disse 0")
}